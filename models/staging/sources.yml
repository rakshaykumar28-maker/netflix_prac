sources:
  - name: NETFLIX_RAW
    database: NETFLIX_DB
    schema: RAW
    tables:
      - name: NETFLIX_TITLES_CLASS
        description: "Unique identifier for each title."
        tests:
          - not_null
          - unique